#include <atcoder/all>
#include <bits/stdc++.h>

using namespace std;
using namespace atcoder;

using ll = long long int;
using ld = long double;
constexpr ll mod = 1e9 + 7;

int main() {
  ld N,K;
  cin >> N >> K;
  vector<ld> A(N);
  for (int i = 0; i < N; ++i) {
    cin >> A[i];
  }

  
}
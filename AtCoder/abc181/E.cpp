#include <atcoder/all>
#include <bits/stdc++.h>

using namespace std;
using namespace atcoder;

using ll = long long int;

int main() {
  int N,M;
  cin >> N >> M;
  vector<int> H(N), W(M);
  sort(H.begin(), H.end());
  sort(W.begin(), W.end());
}